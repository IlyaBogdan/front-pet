interface IUserDialogsData {

}

/**
 * React at broker message 'userDIalogs'
 * 
 * @param {IUserDialogsData} body 
 */
export const userDialogs = (body: IUserDialogsData) => {
    return new Promise((resolve, reject) => {
        
    });
};

// TODO: move it to component
/**
 * 
 * self.chats = body.chats;
 */